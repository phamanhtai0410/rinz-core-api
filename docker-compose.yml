version: "3"

services:
  api:
    container_name: rinz-music
    build: .
    restart: always
    volumes:
      - ./:/webapps
    ports:
      - 6002:5000
    environment:
      - PYTHONUNBUFFERED=true
    env_file:
      - .env
    command: python manage.py run
    # command: tail -f /dev/null
